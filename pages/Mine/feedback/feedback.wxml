<view wx:if="{{!needshowInfo}}" class="container">
<form  bindsubmit="formSubmit">
  <!--<block wx:for="{{feed}}">
    <view class="item-wrapper">
          <view class="list" bindtap="check" data-item="{{index}}">
        <text>{{item.title}}</text>
        <image src="{{item.src1}}" wx:if="{{check}}"></image>
        <image src="{{item.src2}}" wx:if="{{checked}}"></image>
      </view>
    </view>
  </block>-->


  <!--test-->
      <checkbox-group class="item-wrapper" bindchange="checkboxChange">
        <label class="list" wx:for="{{feed}}" bindtap="change_status" data-index="{{index}}">           
        <text>{{item.title}}</text><checkbox value="{{item.title}}" checked="{{item.isChecked}}" style="display:none;"/>
        <image bindcatch="change_status" data-index="{{index}}" src="{{item.src2}}" wx:if="{{item.isChecked}}"></image>
        <image bindcatch="change_status" data-index="{{index}}" src="{{item.src1}}" wx:else></image>
      </label>
    </checkbox-group>
<!--test-->

  <!--输入框开始-->
    <view class="item-wrapper">
  <view class="list" bindtap="check_others">
    <text>其他</text>
    <image src="../../../images/feed-choosed.png" wx:if="{{isOtherCheck}}"></image>
    <image src="../../../images/feed-unchoosed.png" wx:else></image>
  </view>
  <textarea class="input" placeholder="请输入内容" placeholder-class="input-txt" auto-focus="true" bindblur="bindTextAreaBlur"/>
</view>
  <!--输入框结束-->

  <!--分割线开始-->
  <view style="width:100%;height:15rpx;background:#f1f1f1;"></view>

  <!--分割线结束-->
  <view class="item-wrapper">
    <view class="item">
      <text>姓名</text>
      <input placeholder="请输入您的姓名" placeholder-style="font-size:30rpx;color:#b4b4b4;" bindinput="bindNameInput" />
    </view>
  </view>
  <view class="item-wrapper">
    <view class="item" style="border-bottom:none;">
      <text>电话</text>
      <input placeholder="请输入您的电话，我们可能联系您" placeholder-style="font-size:30rpx;color:#b4b4b4;" bindinput="bindPhoneNumberInput"/>
    </view>
  </view>
  <button class="submit" form-type="submit">提交</button>
</form>
</view>

<!--info -->
<view wx:else id="info">
<loading hidden="{{loadingHidden}}">
    提交中...
</loading>
  <form bindsubmit="formSubmit" bindreset="formReset">
    <view class="form-item" style="margin-top:20rpx;">
      <view class="form-left">
        <text>*</text>姓 名:</view>
      <view class="form-right">
        <input name="userName" type="text" />
      </view>
    </view>
    <view class="form-item" style="margin-top:20rpx;">
      <view class="form-left">
        <text>*</text>联系电话:</view>
      <view class="form-right">
        <input name="userPhoneNumber" bindinput="bindKeyInput" />
      </view>
    </view>
    <view class="form-item" style="margin-top:20rpx;">
      <view class="form-left">
        <text>*</text>手机验证:</view>
      <view class="form-right other-right">
        <input name="vertifyCode" type="text" class='phone-code' />
        <view bindtap="getCode" wx:if="{{isGetCodeEnable}}" class="get-code">获取验证码</view>
        <view wx:else class="get-code-disable">{{timeleft}}秒后重新获取</view>
      </view>
    </view>
    <view class="form-item" style="margin-top:20rpx;">
      <view class="form-left">
        <text>*</text>所在区域:</view>
      <view class="form-right other-right">
        <input name="province" type="text" class="province" />省
        <input name="city" type="text" class="area" />市</view>
    </view>

    <!--分割线开始-->
    <view style="width:100%;height:15rpx;background:#f1f1f1;"></view>

    <!--分割线结束-->
    <view class="form-item" style="margin-top:20rpx;">
      <view class="form-left" style="margin-left:5rpx;">客户服务码:</view>
      <view class="form-right">
        <input name="serviceCode" type="text" />
      </view>
    </view>
    <view class="" style="border-top:1rpx solid #e8e8e8;width:676rp;margin:auto;">
      <view class="form-left" style="margin-top:10rpx;width:100%;">
        <text>*</text>你想了解哪些展位信息(可多选):</view>
      <view class="form-down">
        <checkbox-group name="checkBoxGroup" bindchange="checkboxChange" class="check-box">
          <block wx:for="{{feed}}">
          <label class="checkbox">
            <checkbox value="{{item.title}}" checked="{{item.isChecked}}" size="20" />{{item.title}}
          </label>
          </block>
        </checkbox-group>
      </view>
      <button formType="submit" class="submit">提交信息</button>
      <view class="tips">完善资料后可查看更多详细信息</view>
    </view>
  </form>

</view>